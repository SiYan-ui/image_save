这是我的 Android 项目约束，请严格遵守，不要自行升级版本，不要使用已经废弃的 API：

1. 语言：Kotlin
   UI 使用：XML View
   说明：当前项目使用 Kotlin 编写 Activity 和业务代码，界面文件位于
   app/src/main/res/layout/activity_main.xml，暂未使用 Jetpack Compose。

2. SDK 配置：
   minSdk = 24
   targetSdk = 37
   compileSdk = 37

   注意：原始需求中写的是 targetSdk=34、compileSdk=34，
   但当前 app/build.gradle.kts 实际配置为 targetSdk=37、compileSdk=37。
   请不要擅自修改 SDK 版本；如需要改为 34，请先明确告知我。

3. AGP 版本：9.3.0
   Gradle 版本：9.5.0

   版本来源：
   - gradle/libs.versions.toml：agp = "9.3.0"
   - gradle/wrapper/gradle-wrapper.properties：
     distributionUrl 使用 gradle-9.5.0-bin.zip

4. 项目结构：
   当前项目实际为单 Module 项目，只有主模块 app。
   当前不存在 img_stay 模块。

   代码默认写在：
   app/src/main/java/com/example/image_save/

   如果确实需要新增 img_stay 模块，请先告诉我，不要自行创建。
   如果新增模块，必须同步修改 settings.gradle.kts 和相关 Gradle 配置。

5. 已有的第三方依赖：

   app/build.gradle.kts 中当前 dependencies 块为：

   dependencies {
       implementation(libs.androidx.appcompat)
       implementation(libs.androidx.core.ktx)
       implementation(libs.material)
       testImplementation(libs.junit)
       androidTestImplementation(libs.androidx.espresso.core)
       androidTestImplementation(libs.androidx.junit)
   }

   对应版本为：

   - androidx.appcompat:appcompat:1.6.1
   - androidx.core:core-ktx:1.10.1
   - com.google.android.material:material:1.10.0
   - junit:junit:4.13.2
   - androidx.test.ext:junit:1.1.5
   - androidx.test.espresso:espresso-core:3.5.1

   不要随便新增依赖。如果确实需要新增依赖，必须先说明：
   - 依赖名称和版本；
   - 使用目的；
   - 是否可以使用现有 Android SDK 或已有依赖替代；
   - 需要修改的完整 Gradle 配置。

6. 网络与存储：

   网络：当前项目未发现网络请求、网络客户端或网络相关依赖，
   默认按“不需要网络”处理。

   存储：项目名称为 image_save，可能需要读写图片文件，
   但当前代码中尚未发现实际的图片读写实现。
   如果实现图片保存功能，应优先使用符合当前 targetSdk 的现代存储方案，
   例如 MediaStore 或应用专属存储目录，不要使用已经废弃的存储 API。

   如果需要访问用户选择的图片，优先使用系统文件选择器或系统照片选择器，
   尽量避免申请不必要的存储权限。

   如果确实需要申请危险权限：
   - 必须在 AndroidManifest.xml 中声明；
   - 必须在运行时动态申请；
   - 必须根据 minSdk 和 targetSdk 选择对应权限；
   - 必须处理用户拒绝、永久拒绝和权限已授予等情况；
   - 不要为了保存应用自己生成的图片而申请不必要的广泛存储权限。

7. 约束：

   ① 不要使用废弃 API。
      如果某个 API 在当前 SDK 中已废弃，必须使用官方推荐的替代方案。
      如因兼容 minSdk=24 必须使用旧 API，需要封装版本判断，
      并说明原因。

   ② 权限要适配当前 targetSdk=37。
      危险权限必须动态申请。
      只申请实现功能所必需的最小权限。

   ③ 不要随便新增依赖。
      如果需要新增依赖，必须先告诉我并说明原因、版本和影响。
      未经确认不要修改依赖版本，也不要将已有依赖升级到新版本。

   ④ 给出完整、可粘贴的代码，并标明每个文件路径。
      对于新增文件，请明确标注“新建文件”。
      对于修改文件，请给出修改后的完整内容，避免只给无法定位的代码片段。

   ⑤ 如果修改 Gradle 配置，请完整给出修改后的代码片段，
      不要只给零散代码。
      不要修改 AGP、Gradle、Kotlin、compileSdk、targetSdk 或 minSdk 版本，
      除非我明确要求。

   ⑥ 当前界面使用 XML View，不要擅自迁移到 Jetpack Compose。
      如需修改界面，优先修改：
      app/src/main/res/layout/activity_main.xml

   ⑦ 当前 Activity 使用 AppCompat：
      app/src/main/java/com/example/image_save/MainActivity.kt

   ⑧ 当前包名和命名空间为：
      com.example.image_save

   ⑨ 业务代码位于：
      app/src/main/java/com/example/image_save/EventEngine.kt

   ⑩ 修改后应检查：
      - Gradle 配置是否可以同步；
      - Kotlin 代码是否可以编译；
      - XML 资源 ID 是否与 Kotlin 代码一致；
      - AndroidManifest.xml 中的 Activity 和权限是否正确；
      - 是否引入了废弃 API；
      - 是否满足 minSdk=24 的兼容性要求。