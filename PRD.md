# 虚拟养育存钱 App 产品需求文档

## 文档信息

- 文档版本：V1.0
- 文档状态：第一版开发依据
- 产品形态：Android 本地应用
- 数据范围：仅保存在用户设备本地
- 网络要求：第一版不需要联网
- 账号要求：第一版不需要登录

## 1. 产品目标和使用场景

### 1.1 产品目标

帮助年轻人建立持续存钱的理由和习惯。

产品通过模拟养育婴儿或宠物，让用户在应用中承担持续发生的消耗责任。用户在现实中将对应金额存入银行卡或支付宝小荷包，再在应用中确认本次存钱，应用负责记录累计存钱金额和账单。

### 1.2 核心使用场景

1. 用户想开始存钱，但缺少持续、具体的动力。
2. 用户创建一个虚拟养育对象，例如婴儿、猫或狗。
3. 应用按真实时间消耗奶粉、纸尿裤、猫粮、猫砂或狗粮。
4. 消耗品用完后，应用产生购买事件。
5. 用户在现实中完成存钱后，在应用内点击确认。
6. 如果用户没有及时确认，系统自动完成虚拟购买，并形成一笔无利率的虚拟借贷。
7. 用户之后可以分次偿还借贷；实际偿还的金额才计入累计存钱总额。

### 1.3 产品边界

- 一个用户第一版只能养育一个对象。
- 对象创建后第一版不允许更换对象。
- 第一版只实现五类周期性消耗品。
- 生病、打针、吃药、疫苗等非周期性突发事件暂不实现。

## 2. 整体页面框架和导航结构

第一版采用“单主界面 + 弹窗”的结构，不设置底部导航栏，不设置复杂的多级页面。

```text
首次打开
  └─ 空白首页
       └─ 选择养育对象
            └─ 输入对象名称
                 └─ 养育主界面
                      ├─ 购买事件区域
                      ├─ 当前借贷弹窗
                      ├─ 偿还金额弹窗
                      └─ 账单弹窗
```

### 2.1 页面和弹窗清单

| 页面或弹窗 | 进入方式 | 退出方式 |
|---|---|---|
| 空白首页 | 首次打开且没有对象 | 点击加号进入选择页 |
| 选择养育对象页 | 空白首页点击加号 | 选择对象后进入命名页 |
| 命名页 | 选择对象后进入 | 点击开始养育进入主界面 |
| 养育主界面 | 创建对象后、重新打开 App | 通过系统返回或关闭弹窗 |
| 借贷列表弹窗 | 点击主界面的当前借贷金额 | 关闭弹窗 |
| 偿还金额弹窗 | 借贷列表或账单中点击偿还 | 取消或确认偿还 |
| 账单弹窗 | 点击主界面账单按钮 | 关闭弹窗 |

### 2.2 固定导航规则

- 主界面是创建对象后的默认入口。
- 账单从主界面进入，不单独占用底部导航位置。
- 当前借贷金额既是状态展示，也是进入借贷列表的操作入口。
- 第一版不提供更换对象、删除对象或重新开始按钮。

## 3. 首页的内容结构

养育主界面使用温馨的家庭场景作为背景，例如客厅、卧室、猫窝或狗窝。

### 3.1 顶部金额区

右上角固定显示：

```text
累计存钱
¥ 1,280.00

当前借贷
¥ 80.00
```

规则：

- 用户直接确认“已存钱”后，金额立即计入累计存钱。
- 虚拟借贷产生时，不增加累计存钱。
- 用户偿还借贷后，实际偿还金额增加到累计存钱。
- 当前借贷金额为所有未偿还借贷的剩余金额之和。
- 没有借贷时，当前借贷显示为 `¥ 0.00`，或按视觉设计隐藏该行但不能隐藏数据。

### 3.2 角色区

显示：

- 养育对象形象；
- 用户设置的对象名称；
- 简短的状态文字。

对象类型：

- 婴儿：奶粉、纸尿裤；
- 猫：猫粮、猫砂；
- 狗：狗粮。

### 3.3 消耗品状态区

每种适用消耗品显示一个状态模块，至少包含：

- 消耗品名称；
- 当前库存数量；
- 计量单位；
- 预计耗尽时间或剩余时间；
- 是否存在待处理购买事件。

示例：

```text
奶粉
剩余 1 罐
预计还有 3 天用完
```

### 3.4 事件区

没有事件时显示当前养育状态。

发生购买事件时显示：

```text
奶粉已经用完了
需要购买 1 罐奶粉
存入 ¥180.00 后完成记录

[已存钱]
```

用户点击“已存钱”后：

- 记录一笔直接存钱账单；
- 累计存钱增加预设金额；
- 当前库存补充一件；
- 消耗重新开始；
- 事件标记为已完成。

### 3.5 账单入口

主界面提供“账单”按钮。点击后打开账单弹窗，不离开主界面上下文。

## 4. 每个模块的用途

### 4.1 对象选择模块

用途：让用户决定要养育哪一种虚拟对象。

第一版只提供婴儿、猫、狗三个选项，并展示各自对应的消耗品。

### 4.2 对象命名模块

用途：让用户为养育对象建立个性化称呼。

创建对象时必须设置名称。名称不能为空，长度限制由实现确定，但需要避免超出主界面布局。

### 4.3 角色展示模块

用途：持续提供养育感和陪伴感，是主界面的视觉中心。

模块显示角色形象、名称和简短状态文字。

### 4.4 消耗品库存模块

用途：展示消耗进度，让用户知道下一笔存钱任务何时产生。

库存和消耗进度由系统自动计算，用户不手动编辑库存。

### 4.5 购买事件模块

用途：把消耗品用完转换为用户需要完成的存钱任务。

事件必须关联具体消耗品、单位和预设价格。

### 4.6 累计存钱模块

用途：展示用户已经实际确认或偿还的存钱总额。

虚拟借贷产生时不能直接增加该金额。

### 4.7 虚拟借贷模块

用途：处理用户没有及时确认存钱时的代购结果。

系统自动购买一件消耗品并创建无利率借贷。消耗流程不能因为借贷而停止。

### 4.8 借贷偿还模块

用途：允许用户分次偿还虚拟借贷。

用户可自定义每次偿还金额，金额精确到小数点后两位，不能超过该笔借贷剩余金额。

### 4.9 账单模块

用途：提供消费、借贷和偿还记录的可追溯明细。

账单默认按时间倒序显示。

### 4.10 通知模块

用途：在 App 未打开时提醒用户消耗品已用完。

每个购买事件只发送一次系统通知。通知不代替本地数据记录，系统仍需在本地保存事件和借贷状态。

## 5. 每个模块第一版最必要的数据和操作

### 5.1 养育对象数据

必要数据：

- `objectType`：`baby`、`cat`、`dog`；
- `objectName`：用户设置的名称；
- `createdAt`：创建时间；
- `backgroundId`：对应家庭背景；
- `isCreated`：是否已创建对象。

必要操作：

- 创建对象；
- 保存对象名称；
- 读取对象信息；
- 重启后恢复对象信息。

### 5.2 消耗品参数数据

参数必须放在独立参数文件中，便于后续维护，不应散落在界面代码中。

每种消耗品至少需要：

- `itemId`：唯一标识；
- `objectType`：适用对象；
- `name`：显示名称；
- `unit`：罐、包等单位；
- `consumptionDuration`：一单位的预计消耗时长；
- `price`：系统预设单价；
- `enabled`：第一版是否启用。

第一版启用的消耗品：

| 对象 | 消耗品 | 单位 |
|---|---|---|
| 婴儿 | 奶粉 | 罐 |
| 婴儿 | 纸尿裤 | 包 |
| 猫 | 猫粮 | 袋或包 |
| 猫 | 猫砂 | 包 |
| 狗 | 狗粮 | 袋或包 |

价格和消耗速度使用第一版估算值。用户不能在 App 内修改这些参数。

### 5.3 库存数据

每种适用消耗品至少需要：

- `itemId`；
- `quantity`：当前库存数量；
- `startedAt`：当前单位开始消耗的时间；
- `expectedDepletedAt`：预计耗尽时间；
- `lastUpdatedAt`：最近一次计算时间。

初始规则：

- 创建对象时，每种适用消耗品赠送一件；
- 从对象创建时间开始消耗；
- 库存消耗完后产生购买事件；
- 直接存钱或系统代购后补充一件库存。

### 5.4 购买事件数据

每个事件至少需要：

- `eventId`；
- `itemId`；
- `triggeredAt`：事件触发时间；
- `amount`：事件发生时的预设金额；
- `status`：待确认、已直接存钱、已转借贷；
- `notificationSent`：是否已经发送通知；
- `completedAt`：完成或转借贷的时间。

必要操作：

- 根据真实时间计算事件是否到期；
- 生成购买事件；
- 用户确认直接存钱；
- 未确认时自动转为借贷；
- 防止同一事件重复计账。

### 5.5 借贷数据

每笔借贷至少需要：

- `debtId`；
- `sourceEventId`；
- `itemId`；
- `createdAt`；
- `originalAmount`；
- `repaidAmount`；
- `remainingAmount`；
- `status`：未偿还、部分偿还、已偿还。

必要操作：

- 创建借贷；
- 计算所有未偿还借贷合计；
- 录入部分偿还；
- 全额偿还后关闭借贷；
- 保留已经偿还的历史记录。

### 5.6 账单数据

每条账单至少需要：

- `recordId`；
- `createdAt`；
- `itemId`；
- `itemNameSnapshot`：记录发生时的名称快照；
- `amount`；
- `recordType`：直接存钱、虚拟借贷、借贷偿还；
- `relatedEventId` 或 `relatedDebtId`；
- `status`；
- `repaidAmount`（借贷记录需要）。

账单必须显示：

- 日期；
- 消耗品名称；
- 金额；
- 记录类型；
- 借贷偿还状态；
- 部分偿还时的“已偿还金额 / 原借贷金额”。

### 5.7 汇总数据

系统需要能够从本地记录得到：

- `totalSavedAmount`：累计存钱总额；
- `totalOutstandingDebt`：当前借贷总额。

推荐将汇总值作为可重算数据保存或缓存，但最终结果必须能够由账单和偿还记录校验。

## 6. 首页和各模块之间的关系

```text
主界面
  ├─ 角色区 ← 养育对象数据
  ├─ 消耗品状态区 ← 消耗品参数 + 库存数据 + 当前时间
  ├─ 事件区 ← 购买事件数据
  ├─ 累计存钱 ← 直接存钱账单 + 借贷偿还账单
  ├─ 当前借贷 ← 未偿还借贷数据
  └─ 账单按钮 → 账单模块

消耗品库存耗尽
  └─ 产生购买事件
       ├─ 用户确认 → 直接存钱账单 → 增加累计存钱 → 补充库存
       └─ 用户未确认 → 虚拟借贷 → 发送一次通知 → 补充库存

当前借贷
  ├─ 点击主界面金额 → 借贷列表
  └─ 点击账单中的借贷 → 偿还弹窗 → 借贷减少、累计存钱增加
```

### 6.1 时间计算关系

- 消耗进度以时间戳计算，不依赖 App 是否一直运行。
- App 每次启动、回到前台和定时任务执行时，都需要根据当前时间补算库存和事件状态。
- 用户长时间未打开 App 时，期间每个实际到期周期都要生成对应记录，不合并成一笔。
- 事件、借贷和库存更新必须具备幂等性，避免重复启动或重复通知导致重复计账。

## 7. 本地数据文件保存需求

### 7.1 总体要求

- 所有业务数据必须保存在用户设备本地。
- 第一版不使用服务器、云数据库、登录账号或联网同步。
- 关闭 App、系统回收 App、重新打开 App 后，养育对象、库存、事件、借贷、账单和汇总金额不能丢失。
- 具体使用 JSON 文件、本地数据库或其他本地持久化方式由开发实现决定，但必须满足本 PRD 的数据和恢复要求。

### 7.2 参数文件

消耗品参数必须独立维护，至少包括：

- 消耗品名称；
- 适用对象；
- 单位；
- 单位消耗速度；
- 单价；
- 是否启用。

后续调整价格或消耗速度时，应只需要修改参数文件或对应的数据层配置，不需要修改页面交互逻辑。

### 7.3 时间和恢复

- 所有关键时间使用可持久化的绝对时间记录。
- App 重启后不能从“重新启动时间”开始计算，而必须从上一次保存状态和当前真实时间继续计算。
- 数据保存需要避免在写入过程中留下无法读取的半成品数据。
- 发生数据读取异常时，应保留可诊断信息，并避免静默覆盖用户已有数据。

### 7.4 通知数据

本地数据中需要保存每个事件是否已经发送通知，确保同一个事件只通知一次。

## 8. 第一版必须完成的功能

1. 首次打开时创建婴儿、猫或狗三种对象之一。
2. 用户可以为对象命名。
3. 创建对象后自动赠送每种适用消耗品一件。
4. 显示角色形象、名称和温馨家庭背景。
5. 实现奶粉、纸尿裤、猫粮、猫砂、狗粮五类周期性消耗品。
6. 消耗品名称、单位、消耗速度、价格和适用对象使用参数配置。
7. 消耗品按照真实时间消耗，App 未打开时也继续计算。
8. 消耗品用完后产生购买事件。
9. 用户点击“已存钱”后，按系统预设金额记录直接存钱并补充库存。
10. 用户未处理购买事件时，系统自动完成虚拟购买并形成无利率借贷。
11. App 未打开时到达事件触发时间，发送一次系统通知。
12. 用户可以分次偿还借贷，金额精确到小数点后两位。
13. 直接存钱和借贷偿还金额计入累计存钱总额。
14. 虚拟借贷产生时不计入累计存钱总额。
15. 主界面右上角显示累计存钱和当前借贷金额。
16. 主界面提供账单入口。
17. 账单显示日期、消耗品名称、金额、记录类型和偿还状态。
18. 部分偿还显示“已偿还金额 / 原借贷金额”。
19. 长时间未打开 App 时，按实际发生次数生成多笔借贷和账单。
20. 重启 App 后数据、状态和时间进度能够恢复。

## 9. 第一版暂时不做的功能

- 同时养育多个对象；
- 创建后更换养育对象；
- 删除对象或重置养育进度；
- 用户在 App 内修改价格、单位或消耗速度；
- 生病、打针、吃药、疫苗等突发事件；
- 其他未列出的消耗品；
- 登录、注册和账号体系；
- 云端保存、跨设备同步和服务器数据库；
- 社交、好友、排行榜和分享；
- 成就、签到、积分、商城和广告；
- 账单筛选、搜索、图表统计和导出；
- 多语言和复杂主题切换；
- 养育对象更换、迁移或恢复到其他设备。

## 10. 可以实际检查的验收标准

### 10.1 创建对象

- [ ] 全新安装或清空本地数据后，打开 App 能看到空白首页和加号。
- [ ] 点击加号后能看到婴儿、猫、狗三个选项。
- [ ] 选择对象后能输入名称。
- [ ] 名称为空时不能完成创建。
- [ ] 创建完成后主界面显示正确的对象形象、名称和对应背景。
- [ ] 创建后不能通过第一版界面更换对象。

### 10.2 初始库存和消耗

- [ ] 创建婴儿后有一件奶粉和一件纸尿裤。
- [ ] 创建猫后有一件猫粮和一件猫砂。
- [ ] 创建狗后有一件狗粮。
- [ ] 每种消耗品显示正确名称、单位和库存。
- [ ] 按参数文件中的消耗速度计算预计耗尽时间。
- [ ] 关闭 App 并等待一段时间后重新打开，库存进度与真实经过时间一致。

### 10.3 直接存钱事件

- [ ] 消耗品到期后主界面出现对应购买事件。
- [ ] 事件显示正确的消耗品、单位和预设金额。
- [ ] 点击“已存钱”后累计存钱增加预设金额。
- [ ] 点击后不会重复创建同一条账单。
- [ ] 点击后库存补充一件并开始下一轮消耗。

### 10.4 通知和虚拟借贷

- [ ] App 未打开时，消耗品到期能够产生对应事件。
- [ ] 同一个事件最多发送一次系统通知。
- [ ] 未确认事件会形成一笔虚拟借贷。
- [ ] 虚拟借贷产生时累计存钱不增加。
- [ ] 虚拟购买后库存得到补充，消耗继续进行。
- [ ] 用户长时间未打开 App 时，每个实际发生的周期都能形成独立记录。

### 10.5 借贷偿还

- [ ] 点击主界面的当前借贷金额能打开借贷列表。
- [ ] 点击账单中的借贷记录能打开偿还入口。
- [ ] 偿还金额支持小数点后两位。
- [ ] 输入 0、负数或超过剩余借贷金额的数值时不能确认。
- [ ] 部分偿还后，借贷剩余金额正确减少。
- [ ] 部分偿还金额正确增加到累计存钱总额。
- [ ] 全额偿还后，该笔借贷状态变为已偿还。

### 10.6 账单

- [ ] 点击账单按钮能打开账单弹窗。
- [ ] 账单按时间倒序显示。
- [ ] 直接存钱记录显示日期、消耗品、金额和直接存钱类型。
- [ ] 虚拟借贷记录显示日期、消耗品、金额和借贷类型。
- [ ] 借贷部分偿还时显示“已偿还金额 / 原借贷金额”。
- [ ] 借贷全部偿还后仍保留历史账单。

### 10.7 数据持久化

- [ ] 关闭 App 后重新打开，对象和名称不丢失。
- [ ] 重新打开后库存和消耗进度不重置。
- [ ] 累计存钱金额不丢失。
- [ ] 当前借贷金额不丢失。
- [ ] 账单记录不丢失。
- [ ] 设备重启后以上数据仍能恢复。
- [ ] 应用没有网络时，核心功能仍可使用。

### 10.8 第一版范围检查

- [ ] 第一版没有同时养育多个对象的入口。
- [ ] 第一版没有更换对象的入口。
- [ ] 第一版没有突发疾病、疫苗或药品事件。
- [ ] 第一版没有登录、联网同步或云数据库依赖。
- [ ] 第一版的价格和消耗速度可以通过参数配置维护，而不是散落在页面代码中。
